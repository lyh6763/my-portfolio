# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: 스타벅스 코리아 (LOVE ME 봄 시즌 프로모션)
- **분석 날짜**: 2025-12-12
- **적용 프로젝트**: 포트폴리오 웹사이트
- **테마 컨셉**: 봄 시즌 / 로맨틱 / 페미닌 / 파스텔

---

## 🎯 CSS 변수 정의

```css
:root {
  /* ========================================
     Primary Colors - 라일락 퍼플 계열
     ======================================== */
  --color-primary: #9B7BB8;
  --color-primary-light: #C4A8D8;
  --color-primary-dark: #7B5BA0;

  /* ========================================
     Secondary Colors - 스트로베리 & 액센트
     ======================================== */
  --color-secondary: #E84B5B;
  --color-secondary-light: #F8A5A5;
  --color-secondary-dark: #D43F4F;
  --color-accent: #8B6914;
  --color-accent-light: #B8923A;
  --color-accent-dark: #6B4E12;

  /* ========================================
     Background Colors - 봄 핑크 계열
     ======================================== */
  --color-bg-primary: #FDF0F0;
  --color-bg-secondary: #FCE4E8;
  --color-bg-tertiary: #FFF5F5;
  --color-bg-card: #FFFFFF;
  --color-bg-overlay: rgba(253, 240, 240, 0.95);

  /* ========================================
     Text Colors
     ======================================== */
  --color-text-primary: #2D2D2D;
  --color-text-secondary: #6B6B6B;
  --color-text-muted: #9B9B9B;
  --color-text-heading: #8B2332;
  --color-text-inverse: #FFFFFF;

  /* ========================================
     Interactive Colors - 버튼, 링크
     ======================================== */
  --color-button-primary: #9B7BB8;
  --color-button-primary-hover: #7B5BA0;
  --color-button-secondary: #E84B5B;
  --color-button-secondary-hover: #D43F4F;
  --color-link: #8B2332;
  --color-link-hover: #6B1322;

  /* ========================================
     Decorative Colors - 장식 요소
     ======================================== */
  --color-cherry-blossom: #F5C6D0;
  --color-petal: #FADCE3;
  --color-cream: #F5E6D3;

  /* ========================================
     Utility Colors - 상태 표시
     ======================================== */
  --color-success: #5CB85C;
  --color-warning: #F0AD4E;
  --color-error: #D9534F;
  --color-info: #5BC0DE;

  /* ========================================
     Border & Shadow
     ======================================== */
  --color-border: #F0E0E0;
  --color-border-light: #FAF0F0;
  --color-shadow: rgba(155, 123, 184, 0.15);
  --color-shadow-hover: rgba(155, 123, 184, 0.25);
}
```

---

## 🎨 컬러 사용 가이드

### 1. Primary Color (라일락 퍼플)
| 변수 | 사용 용도 |
|------|-----------|
| `--color-primary` | 메인 CTA 버튼, 네비게이션 활성 상태, 아이콘 |
| `--color-primary-light` | 버튼 비활성 상태, 태그 배경, 하이라이트 |
| `--color-primary-dark` | 버튼 호버 상태, 포커스 링, 강조 텍스트 |

```css
/* 사용 예시 */
.btn-primary {
  background-color: var(--color-primary);
  color: var(--color-text-inverse);
}

.btn-primary:hover {
  background-color: var(--color-primary-dark);
}
```

### 2. Secondary Color (스트로베리 레드)
| 변수 | 사용 용도 |
|------|-----------|
| `--color-secondary` | 보조 버튼, 알림 배지, 특별 프로모션 |
| `--color-secondary-light` | 에러 메시지 배경, 세일 태그 |
| `--color-secondary-dark` | 중요 알림, 긴급 표시 |

```css
/* 사용 예시 */
.badge-new {
  background-color: var(--color-secondary);
  color: var(--color-text-inverse);
}

.alert-important {
  border-left: 4px solid var(--color-secondary);
}
```

### 3. Background Colors
| 변수 | 사용 용도 |
|------|-----------|
| `--color-bg-primary` | 페이지 전체 배경 |
| `--color-bg-secondary` | 섹션 구분, 푸터 배경 |
| `--color-bg-card` | 카드, 모달, 드롭다운 |

```css
/* 사용 예시 */
body {
  background-color: var(--color-bg-primary);
}

.card {
  background-color: var(--color-bg-card);
  box-shadow: 0 4px 12px var(--color-shadow);
}
```

### 4. Text Colors
| 변수 | 사용 용도 |
|------|-----------|
| `--color-text-primary` | 본문 텍스트, 일반 콘텐츠 |
| `--color-text-secondary` | 부가 설명, 캡션 |
| `--color-text-heading` | 제목, 중요 헤딩 (버건디) |
| `--color-text-muted` | 비활성 텍스트, 플레이스홀더 |

```css
/* 사용 예시 */
h1, h2, h3 {
  color: var(--color-text-heading);
}

p {
  color: var(--color-text-primary);
}

.caption {
  color: var(--color-text-muted);
}
```

---

## 📱 반응형 고려사항

### 다크모드 지원

```css
/* 다크모드 컬러 팔레트 */
@media (prefers-color-scheme: dark) {
  :root {
    /* Background - 어두운 톤으로 전환 */
    --color-bg-primary: #1A1A2E;
    --color-bg-secondary: #16213E;
    --color-bg-tertiary: #0F0F1A;
    --color-bg-card: #252540;

    /* Text - 밝은 톤으로 전환 */
    --color-text-primary: #E8E8E8;
    --color-text-secondary: #B8B8B8;
    --color-text-muted: #888888;
    --color-text-heading: #F5C6D0;

    /* Primary - 약간 밝게 조정 */
    --color-primary: #B89BD8;
    --color-primary-light: #D4C4E8;
    --color-primary-dark: #9B7BB8;

    /* Border & Shadow */
    --color-border: #3A3A5A;
    --color-shadow: rgba(0, 0, 0, 0.3);
  }
}
```

### 수동 다크모드 토글

```css
/* 클래스 기반 다크모드 */
[data-theme="dark"] {
  --color-bg-primary: #1A1A2E;
  --color-bg-secondary: #16213E;
  --color-text-primary: #E8E8E8;
  --color-text-heading: #F5C6D0;
  /* ... 나머지 다크모드 변수 */
}
```

### 접근성 고려사항

```css
/* 고대비 모드 */
@media (prefers-contrast: high) {
  :root {
    --color-text-primary: #000000;
    --color-text-heading: #5A0012;
    --color-bg-primary: #FFFFFF;
    --color-primary: #6B4B88;
  }
}

/* 모션 감소 선호 시 */
@media (prefers-reduced-motion: reduce) {
  * {
    transition: none !important;
  }
}
```

### 컬러 대비 체크리스트

| 조합 | 대비율 | WCAG 등급 |
|------|--------|-----------|
| `--color-text-primary` on `--color-bg-primary` | 12.5:1 | AAA |
| `--color-text-heading` on `--color-bg-primary` | 8.2:1 | AAA |
| `--color-text-inverse` on `--color-primary` | 4.8:1 | AA |
| `--color-text-inverse` on `--color-secondary` | 4.5:1 | AA |

---

## 🧩 컴포넌트별 적용 예시

### 네비게이션 바

```css
.navbar {
  background-color: var(--color-bg-card);
  border-bottom: 1px solid var(--color-border);
  box-shadow: 0 2px 8px var(--color-shadow);
}

.nav-link {
  color: var(--color-text-primary);
}

.nav-link:hover,
.nav-link.active {
  color: var(--color-primary);
}
```

### 카드 컴포넌트

```css
.card {
  background-color: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: 12px;
  box-shadow: 0 4px 12px var(--color-shadow);
  transition: box-shadow 0.3s ease;
}

.card:hover {
  box-shadow: 0 8px 24px var(--color-shadow-hover);
}

.card-title {
  color: var(--color-text-heading);
}

.card-description {
  color: var(--color-text-secondary);
}
```

### 버튼 시스템

```css
/* Primary Button */
.btn-primary {
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
  color: var(--color-text-inverse);
  border: none;
  padding: 12px 24px;
  border-radius: 8px;
}

/* Secondary Button */
.btn-secondary {
  background-color: transparent;
  color: var(--color-primary);
  border: 2px solid var(--color-primary);
}

/* Accent Button */
.btn-accent {
  background-color: var(--color-secondary);
  color: var(--color-text-inverse);
}
```

---

## 📋 빠른 복사용 HEX 코드 목록

```
Primary:    #9B7BB8  #C4A8D8  #7B5BA0
Secondary:  #E84B5B  #F8A5A5  #D43F4F
Accent:     #8B6914  #B8923A  #6B4E12
Background: #FDF0F0  #FCE4E8  #FFF5F5
Text:       #2D2D2D  #6B6B6B  #8B2332
Decorative: #F5C6D0  #FADCE3  #F5E6D3
```

---

## 📚 참고 자료

- [WCAG 색상 대비 가이드라인](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html)
- [CSS Custom Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties)
- [prefers-color-scheme](https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-color-scheme)
